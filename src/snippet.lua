---@param level number | `Logger.ERROR` | `Logger.WARNING` | `Logger.INFO` | `Logger.DEBUG` | `Logger.VERBOSE`
local function setLogLevel(level)
    ---@type number
    local logLevel = level

    ---@type string
    local aString = level -- Expect error
end
